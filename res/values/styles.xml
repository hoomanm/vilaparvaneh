<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="button_small" parent="@android:style/Widget.Button">
        <item name="android:paddingLeft">@dimen/app_default_component_margin_large</item>
        <item name="android:paddingRight">@dimen/app_default_component_margin_large</item>
        <item name="android:paddingTop">@dimen/app_default_component_margin_large</item>
        <item name="android:paddingBottom">@dimen/app_default_component_margin_large</item>
    </style>

</resources>